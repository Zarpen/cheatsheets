apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-data
  namespace: default
  labels:
    app: db-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
